apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc-iscsi
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: tns-iscsi
