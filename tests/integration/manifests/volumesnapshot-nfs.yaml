apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: test-snapshot-nfs
spec:
  volumeSnapshotClassName: tns-csi-snapshot-nfs
  source:
    persistentVolumeClaimName: test-pvc-nfs
