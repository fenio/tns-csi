apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc-nvmeof
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  storageClassName: tns-nvmeof
