apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc-dual-nvmeof
  annotations:
    csi.truenas.org/force-format: "true"
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  storageClassName: tns-csi-nvmeof
