apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc-nvmeof-block
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Block
  resources:
    requests:
      storage: 1Gi
  storageClassName: tns-nvmeof
